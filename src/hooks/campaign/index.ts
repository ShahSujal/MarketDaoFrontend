import { EStatus } from "@/types/common";

export const createCampaignHook = async() => {
    
    
    
    
    return {
        status: EStatus.SUCCESS,
        title: "Campaign created",
        description: "Campaign created successfully",
    };
}